# KhamaKey Pages v41

Frontend statico per Cloudflare Pages (`khamakey-app`).

Vedi il README principale nella cartella superiore per deploy completo, SQL e worker.

Configurazione in `config.js`: Supabase, URL worker e URL Pages.
