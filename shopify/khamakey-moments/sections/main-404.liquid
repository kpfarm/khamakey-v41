<section class="page-width section">
  <div class="cart-empty">
    <p class="eyebrow">404</p>
    <h1>{{ 'general.404_title' | t }}</h1>
    <p>{{ 'general.404_text' | t }}</p>
    <div class="btn-group" style="justify-content:center">
      <a class="btn btn--primary" href="{{ routes.root_url }}">{{ 'general.home' | t }}</a>
      <a class="btn btn--ghost" href="{{ routes.all_products_collection_url }}">{{ 'general.products' | t }}</a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
